[package]
name = "cargo-slow"
version = "0.1.0"
edition = "2021"
authors = ["Luca Cappelletti"]
description = "Cargo subcommand to diagnose a slow machine: identify disk, memory, CPU, and thermal issues"
license = "MIT"
repository = "https://github.com/LucaCappelletti94/cargo-slow"
homepage = "https://github.com/LucaCappelletti94/cargo-slow"
documentation = "https://github.com/LucaCappelletti94/cargo-slow#readme"
readme = "README.md"
keywords = ["cargo-subcommand", "monitoring", "diagnostics", "performance", "linux"]
categories = ["command-line-utilities", "development-tools::profiling"]
rust-version = "1.88"
exclude = [
    ".github/",
    "metrics.csv",
    "*.bin",
    "*.png",
]

[[bin]]
name = "cargo-slow"
path = "src/main.rs"

[dependencies]
sha2 = "0.10"
sysinfo = "0.38.1"
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4.5", features = ["derive"] }
chrono = "0.4"
csv = "1.3"
ratatui = "0.30"
crossterm = "0.29"
libc = "0.2"

[dev-dependencies]
cargo-husky = { version = "1", features = ["precommit-hook", "run-cargo-fmt", "run-cargo-clippy"] }

[profile.release]
lto = true
codegen-units = 1
strip = true
